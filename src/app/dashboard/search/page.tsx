import { layout as l } from "@/lib/theme";

export default function SearchPage() {
 return (
 <div className={l.page}>
 <div className={l.pageHeader}>
 <h1 className={l.pageTitle}>Search Documents</h1>
 <p className={l.pageSubtitle}>Search and filter your documents</p>
 </div>
 </div>
 );
}